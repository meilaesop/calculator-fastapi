# Calculator FastAPI App

Simple FastAPI app serving a modern calculator UI from `static/` and exposing `/api/calc`.

Run locally:

```powershell
python Untitled-1.py
# or
uvicorn Untitled-1:app --reload
```

Install deps:

```powershell
pip install -r requirements.txt
```
